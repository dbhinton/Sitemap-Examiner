#!/bin/bash

# This script is used to build your Flask app.
# You can install any necessary libraries or dependencies here.
# Replace the commands below with the actual build commands if needed.

# Example: Install required Python packages using pip
pip install -r requirements.txt

# Print a message indicating successful build
echo "Build completed!"
exit 0